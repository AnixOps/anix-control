# V2Board AnixOps production environment example

# Base
TZ=Asia/Shanghai
VERSION=latest
GIN_MODE=release
PANEL_FRONTEND_PORT=3000
PANEL_API_PORT=8080
PANEL_GRPC_PORT=50051
GRAFANA_PORT=3001
ANSIBLE_HOST_KEY_CHECKING=False
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# Database
DB_USER=v2board
DB_PASSWORD=your_secure_db_password_here
DB_NAME=v2board

# Redis
REDIS_PASSWORD=your_secure_redis_password_here

# Application
# Note:
# - config/config.yaml remains the canonical bootstrap source of truth
# - runtime selection belongs in config/config.yaml.forward_runtime
# - local go run does not auto-load this file unless your shell exports it
JWT_SECRET=your_jwt_secret_at_least_32_chars_here
API_TOKEN=your_api_token_for_node_communication

# Docker image tag for local builds
DOCKER_IMAGE=v2board:latest

# Grafana
GRAFANA_ADMIN=admin
GRAFANA_PASSWORD=your_grafana_password_here

# Optional domain metadata
DOMAIN=panel.example.com
EMAIL=admin@example.com
